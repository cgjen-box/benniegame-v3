---
phase: 01-foundation
plan: 02
type: execute
---

<objective>
Implement the design system: BennieColors and BennieTypography with Color hex extension.

Purpose: Establish the visual foundation that all UI components will use.
Output: Working color and typography systems matching PLAYBOOK_CONDENSED.md specifications.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@SWIFTUI_CODING_GUIDELINES.md
@PLAYBOOK_CONDENSED.md

**Prior plan:** 01-01 created project structure

**Critical color values (source of truth - PLAYBOOK_CONDENSED.md):**
- Bennie Brown: #8C7259 (NO clothing)
- Bennie Tan (snout only): #C4A574
- Bennie Nose: #3D2B1F
- Lemminge Blue: #6FA8DC (NEVER green, NEVER brown)
- Lemminge Pink: #E8A0A0
- Lemminge Belly: #FAF5EB
- Success: #99BF8C
- Coin Gold: #D9C27A
- Wood Light: #C4A574
- Wood Medium: #A67C52
- Wood Dark: #6B4423
- Rope: #B8956B
- Chain: #6B6B6B
- Woodland: #738F66
- Far Trees: #4A6B5C
- Near Foliage: #7A9973
- Sky: #B3D1E6
- Cream: #FAF5EB
- Light Rays: #F5E6C8 (30% opacity)
- Moss: #5D6B4D
- Path Stone: #A8A090
- Text on Wood: #4A4036
- Text Dark: #2D2D2D

**FORBIDDEN (never implement):**
- Pure Red #FF0000
- Pure White #FFFFFF (large areas)
- Pure Black #000000 (large areas)
- Any neon colors
- Saturation > 80%
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Color hex extension</name>
  <files>BennieGame/Core/Utilities/Extensions.swift</files>
  <action>
Create Extensions.swift with Color hex initializer:

```swift
import SwiftUI

extension Color {
    init(hex: String) {
        let hex = hex.trimmingCharacters(in: CharacterSet.alphanumerics.inverted)
        var int: UInt64 = 0
        Scanner(string: hex).scanHexInt64(&int)
        let a, r, g, b: UInt64
        switch hex.count {
        case 6: // RGB
            (a, r, g, b) = (255, int >> 16, int >> 8 & 0xFF, int & 0xFF)
        case 8: // ARGB
            (a, r, g, b) = (int >> 24, int >> 16 & 0xFF, int >> 8 & 0xFF, int & 0xFF)
        default:
            (a, r, g, b) = (255, 0, 0, 0)
        }
        self.init(
            .sRGB,
            red: Double(r) / 255,
            green: Double(g) / 255,
            blue: Double(b) / 255,
            opacity: Double(a) / 255
        )
    }
}
```

This is the foundation for all color definitions.
  </action>
  <verify>Project builds; Color(hex: "8C7259") compiles</verify>
  <done>Color hex extension works and project builds</done>
</task>

<task type="auto">
  <name>Task 2: Create BennieColors.swift with all color definitions</name>
  <files>BennieGame/Design/Theme/BennieColors.swift</files>
  <action>
Create BennieColors.swift with ALL colors from PLAYBOOK_CONDENSED.md.

Use the exact structure from SWIFTUI_CODING_GUIDELINES.md:
- enum BennieColors with static let properties
- Organized into sections: Character, UI, Environment, Text
- Include FORBIDDEN comments at bottom as documentation

CRITICAL: Copy exact hex values from context above. Do NOT guess colors.

Include section headers with box-drawing characters:
```swift
// ═══════════════════════════════════════════════════════════════════
// CHARACTER COLORS (NON-NEGOTIABLE)
// ═══════════════════════════════════════════════════════════════════
```

Every color MUST have a doc comment explaining its use.
  </action>
  <verify>Project builds; BennieColors.bennieBrown compiles; all 22+ colors accessible</verify>
  <done>All colors defined matching playbook, organized with comments</done>
</task>

<task type="auto">
  <name>Task 3: Create BennieTypography.swift</name>
  <files>BennieGame/Design/Theme/BennieTypography.swift</files>
  <action>
Create BennieTypography.swift with SF Rounded font system:

```swift
import SwiftUI

enum BennieFont {
    /// Titles: 32-48pt Bold
    static func title(_ size: CGFloat = 40) -> Font {
        .system(size: size, weight: .bold, design: .rounded)
    }

    /// Body: 17-24pt Regular
    static func body(_ size: CGFloat = 20) -> Font {
        .system(size: size, weight: .regular, design: .rounded)
    }

    /// Buttons: 20-28pt Semibold
    static func button(_ size: CGFloat = 24) -> Font {
        .system(size: size, weight: .semibold, design: .rounded)
    }

    /// Labels: 14-17pt Medium
    static func label(_ size: CGFloat = 16) -> Font {
        .system(size: size, weight: .medium, design: .rounded)
    }

    /// Large numbers (dice, coins): 40-72pt Bold
    static func number(_ size: CGFloat = 56) -> Font {
        .system(size: size, weight: .bold, design: .rounded)
    }
}
```

SF Rounded is built into iOS - no custom fonts needed.
  </action>
  <verify>Project builds; BennieFont.title(48) compiles and returns Font</verify>
  <done>Typography system complete with all font functions</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Project builds without errors
- [ ] `Color(hex: "8C7259")` produces correct brown
- [ ] All 22+ BennieColors properties accessible
- [ ] BennieFont functions return Font type
- [ ] No forbidden colors included (verify no FF0000, FFFFFF, 000000)
</verification>

<success_criteria>
- All 3 tasks completed
- Colors match PLAYBOOK_CONDENSED.md exactly
- Typography uses SF Rounded
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
