---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Create the Xcode project with correct iPad configuration, folder structure, and Lottie dependency.

Purpose: Establish the project foundation that all subsequent phases build upon.
Output: BennieGame.xcodeproj running on iPad simulator with proper folder structure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@SWIFTUI_CODING_GUIDELINES.md

**Tech constraints:**
- iPadOS 17.0+ deployment target
- iPad only (no iPhone support)
- Landscape orientation locked
- Bundle ID: com.bennie.game

**Folder structure from STRUCTURE.md:**
```
BennieGame/
├── App/
├── Core/
│   ├── State/
│   ├── Services/
│   └── Utilities/
├── Features/
├── Design/
│   ├── Theme/
│   ├── Components/
│   ├── Characters/
│   └── Layout/
└── Resources/
    ├── Assets.xcassets/
    ├── Lottie/
    └── Audio/
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with iPad-only configuration</name>
  <files>BennieGame/BennieGame.xcodeproj, BennieGame/Info.plist</files>
  <action>
Create new Xcode project using command line or Xcode template:
- Product Name: BennieGame
- Organization Identifier: com.bennie
- Interface: SwiftUI
- Language: Swift
- Minimum Deployment: iOS 17.0

Configure Info.plist:
- Set UIRequiredDeviceCapabilities to ["ipad"]
- Set UISupportedInterfaceOrientations~ipad to ONLY ["UIInterfaceOrientationLandscapeLeft", "UIInterfaceOrientationLandscapeRight"]
- Remove all portrait orientations
- Set UIRequiresFullScreen to YES
- Set LSRequiresIPhoneOS to YES

DO NOT include iPhone configurations. This is iPad-only.
  </action>
  <verify>
xcodebuild -project BennieGame/BennieGame.xcodeproj -scheme BennieGame -destination 'platform=iOS Simulator,name=iPad (10th generation)' build
  </verify>
  <done>Project builds successfully targeting iPad simulator in landscape only</done>
</task>

<task type="auto">
  <name>Task 2: Create folder structure matching architecture</name>
  <files>BennieGame/App/, BennieGame/Core/, BennieGame/Features/, BennieGame/Design/, BennieGame/Resources/</files>
  <action>
Create the following directory structure inside BennieGame/:

App/
  - BennieGameApp.swift (move existing)
  - AppCoordinator.swift (placeholder)

Core/
  State/ (empty, for Phase 2)
  Services/ (empty, for Phase 5)
  Utilities/
    - Extensions.swift (Color hex extension)

Features/
  Loading/ (empty, for Phase 2)
  PlayerSelection/ (empty)
  Home/ (empty)
  Activities/ (empty)
  Celebration/ (empty)
  Treasure/ (empty)
  Video/ (empty)
  Parent/ (empty)

Design/
  Theme/ (for Plan 01-02)
  Components/ (for Plan 01-03)
  Characters/ (for Phase 2)
  Layout/ (empty)

Resources/
  Assets.xcassets/ (move existing)
  Lottie/ (empty)
  Audio/
    Narrator/
    Bennie/
    Music/
    Effects/

Update Xcode project to include all groups. Ensure folders are added as groups, not folder references.
  </action>
  <verify>ls -R BennieGame/ shows all directories; xcodebuild build still succeeds</verify>
  <done>All directories exist and project compiles with new structure</done>
</task>

<task type="auto">
  <name>Task 3: Add Lottie-iOS dependency via SPM</name>
  <files>BennieGame/BennieGame.xcodeproj/project.pbxproj</files>
  <action>
Add Lottie-iOS package via Swift Package Manager:
- Repository: https://github.com/airbnb/lottie-spm.git
- Version: Up to Next Major (4.0.0)

Verify package resolves and imports work. Create a test file to verify:

```swift
// Test import - can be removed after verification
import Lottie
```

This is the ONLY external dependency for Phase 1.
  </action>
  <verify>
Build project and verify: `import Lottie` compiles without errors
  </verify>
  <done>Lottie-iOS added, imports work, project builds</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project BennieGame/BennieGame.xcodeproj -scheme BennieGame build` succeeds
- [ ] Running on iPad Simulator shows landscape-only
- [ ] Cannot rotate to portrait
- [ ] Folder structure matches architecture spec
- [ ] `import Lottie` works in any Swift file
</verification>

<success_criteria>
- All 3 tasks completed
- Project builds without errors
- iPad landscape-only enforced
- Folder structure complete
- Lottie dependency available
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
